const Home = () => {
  return (
    <section className="hero">
      <div className="hero-content">
        <h2>Moments Fade. Memories Don’t.</h2>
        <p>Let’s capture them — naturally, beautifully, forever.</p>
      </div>
    </section>
  );
};

export default Home;
